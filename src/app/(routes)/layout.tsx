import React from "react";
import Sidebar from "./dashboard/(share)/Sidebar";

const layout = ({ children }: { children: React.ReactNode }) => {
  return <div>{children}</div>;
};

export default layout;
